[18/04/2026]:
  1. Fixed pink-rendering on Android builds (game was fully pink despite working in Editor and Unity Remote). Root cause: 20+ `Shader.Find()` calls across runtime code (Projectile, Entity/HitEffect, Champion, Structure, PlayerController, TargetHighlight, FloatingHealthBar, HumanHealthBar, SkillButton, ChampionStats, AutoAttackButton, SkillAimIndicator, GameBootstrap) returned null in builds — Unity's build analyzer can't detect string-based shader references, so the shaders were stripped and never shipped.
    - Introduced `Pathogen.ShaderLibrary` ScriptableObject singleton at [Assets/Scripts/Pathogen/Core/ShaderLibrary.cs](Assets/Scripts/Pathogen/Core/ShaderLibrary.cs) loaded from [Assets/Resources/ShaderLibrary.asset](Assets/Resources/ShaderLibrary.asset). Holds direct `Shader` references so Unity's build analyzer sees them as used assets. Access pattern: `ShaderLibrary.Instance.xyz`.
    - Refactored all 20+ runtime `Shader.Find()` call sites to use `ShaderLibrary.Instance`. Editor-only code in `Assets/Packages/ai.meshy/` left untouched since it doesn't ship.
    - Created [Assets/Resources/URPUnlit_Anchor.mat](Assets/Resources/URPUnlit_Anchor.mat) — a zero-cost "anchor" material that keeps URP/Unlit's default variant alive through URP's variant stripping. Required because `new Material(shader)` at runtime doesn't signal keyword usage to the stripper.
    - Added `Skybox/Procedural` to `m_AlwaysIncludedShaders` in [ProjectSettings/GraphicsSettings.asset](ProjectSettings/GraphicsSettings.asset) (fixes pink skybox; scene Lighting uses Unity's built-in Default-Skybox material).
    - Re-enabled `m_StripUnusedVariants: 1` in [Assets/Settings/UniversalRenderPipelineGlobalSettings.asset](Assets/Settings/UniversalRenderPipelineGlobalSettings.asset) (was temporarily disabled during diagnosis). Aggressive variant stripping restored → faster builds, smaller APK.
    - Removed HDRP package (`com.unity.render-pipelines.high-definition`) from [Packages/manifest.json](Packages/manifest.json), its entry in `m_RenderPipelineGlobalSettingsMap`, and the auto-generated `Assets/HDRPDefaultResources/` folder. Project is URP-only; HDRP was dead weight (~400MB Library cache).
    - Installed `com.unity.mobile.android-logcat` package for in-Editor device log capture. Confirmed Android Graphics APIs set to **OpenGLES3-only** (Vulkan removed earlier — Mali-G76 on Galaxy S10 has known URP+Vulkan issues).
  2. Imported `Necrova_Idle.fbx` and `Necrova_Run.fbx` as animation-only FBXs retargeted onto the avatar extracted from `Necrova_Walk.fbx` via "Copy From Other Avatar". Mesh sub-assets are unused (stripped from build by reference-culling, Mesh Compression: High for Library/ savings). All three clips: Bake Into Pose ✓ (Rotation / Y=Feet / XZ=Center of Mass) to strip root motion so code drives `transform.position`, Loop Time ✓, Loop Pose ✓.
  3. Rebuilt [Assets/Champions/Necrova/Necrova.controller](Assets/Champions/Necrova/Necrova.controller) into a sub-state machine `Locomotion` containing two 1D Blend Trees — `Walking` (Idle@0, Walk@2) and `Running` (Idle@0, Run@5.5) — gated by `IsRunning` bool with 0.2s crossfade. Each state's Speed Multiplier is wired to `LocomotionMultiplier` float so playback scales with actual velocity and kills foot-sliding at any speed, including the "running-but-slowed" case.
  4. Rewrote [Assets/Scripts/Pathogen/Champions/ChampionAnimator.cs](Assets/Scripts/Pathogen/Champions/ChampionAnimator.cs): replaces the old `IsMoving` bool with damped `Speed` (0.12s damp for natural accel/decel), `LocomotionMultiplier` (= actualSpeed / naturalSpeed), and `IsRunning` (mirrored from `Champion.isRunning`). Constants `WalkNaturalSpeed=2`, `RunNaturalSpeed=5.5` must match Blend Tree thresholds — tune post-playtest if feet slide.
  5. Added `isRunning` bool to [Assets/Scripts/Pathogen/Champions/Champion.cs](Assets/Scripts/Pathogen/Champions/Champion.cs) State section. Game-state driven, not a speed threshold — a champion can be running but moving slowly (slows, terrain). No system flips it yet; wire to out-of-combat timer, move-speed buffs, or skill states when those systems come online.
  6. Structure health bar now clears the cube (heightOffset `2.5 → 4.5` in [Assets/Scripts/Pathogen/Setup/GameBootstrap.cs](Assets/Scripts/Pathogen/Setup/GameBootstrap.cs)) — tilted MOBA camera no longer overlaps the bar with the structure body.
  7. Structure projectile "muzzle-flash" polish in [Assets/Scripts/Pathogen/Entities/Structure.cs](Assets/Scripts/Pathogen/Entities/Structure.cs) + [Assets/Scripts/Pathogen/Combat/StructureProjectile.cs](Assets/Scripts/Pathogen/Combat/StructureProjectile.cs): core sphere now tinted toward white, wrapped in a `slimeBeam`-shaded halo child (2.4× scale, white core + team-tinted edge) that pulses 14 Hz along with the shot. Projectile speed `12 → 7` so the flash reads as it travels. No new shader added — reuses `ShaderLibrary.Instance.slimeBeam` so URP variant stripping stays intact.
  8. Slowed ranged minion projectile speed `12 → 6` in [Assets/Scripts/Pathogen/Entities/Minion.cs](Assets/Scripts/Pathogen/Entities/Minion.cs) `FireRangedAttack`. Attack speed (cooldown) unchanged — projectiles just take longer to reach target, giving dodge/peel windows without changing DPS math.
  9. Floating health bar colour now keys off ally/enemy relation to the local player, not raw team identity — enemies are red, allies are green regardless of which side the player picks. Added `GameManager.playerTeam` (default `Team.Virus`, set from the player champion in [Assets/Scripts/Pathogen/Setup/GameBootstrap.cs](Assets/Scripts/Pathogen/Setup/GameBootstrap.cs)) and read in [Assets/Scripts/Pathogen/UI/FloatingHealthBar.cs](Assets/Scripts/Pathogen/UI/FloatingHealthBar.cs) `CreateHealthBar`. Applies uniformly to minions, structures, champions — same convention everywhere.
  10. Taller Dark Sentinels / Sentinels — cube scale `y: 4 → 7` in [Assets/Scripts/Pathogen/Setup/GameBootstrap.cs](Assets/Scripts/Pathogen/Setup/GameBootstrap.cs). Health bar offset bumped to `5.5` to keep 2u of clearance above the new top. Projectile + aim-line spawn heights in [Assets/Scripts/Pathogen/Entities/Structure.cs](Assets/Scripts/Pathogen/Entities/Structure.cs) now derive from `transform.localScale.y * 0.5` (new `ProjectileSpawnHeight` getter) so they auto-track future height changes — constants removed.
  11. Moved structure-projectile muzzle flash from a programmatic halo to an **Editor-authored Particle System prefab** — the Unity industry-standard pipeline. New pieces:
    - [Assets/Scripts/Pathogen/Core/VFXLibrary.cs](Assets/Scripts/Pathogen/Core/VFXLibrary.cs) — `ScriptableObject` mirroring `ShaderLibrary`, holds `GameObject` prefab refs so build analyzer traces prefab→material→shader→variant and nothing gets stripped. Singleton via `VFXLibrary.Instance`, backed by `Assets/Resources/VFXLibrary.asset`.
    - [Assets/Scripts/Pathogen/Setup/Editor/VFXPrefabBuilder.cs](Assets/Scripts/Pathogen/Setup/Editor/VFXPrefabBuilder.cs) — `MenuItem("Pathogen/Build VFX Prefabs")` generates `Assets/Resources/VFX/StructureMuzzleFlash.{mat,prefab}` (URP Particles/Unlit, Additive blend + Sphere shape + burst-then-rate emission + alpha-in/out gradient + pop-and-fade size curve + subtle 3D noise). Also lazy-creates and auto-wires `VFXLibrary.asset`. Re-runnable — artists can tweak the prefab in Inspector afterwards and the in-Inspector edits stick.
    - [Structure.cs](Assets/Scripts/Pathogen/Entities/Structure.cs) `AttachMuzzleFlash` instantiates the prefab as a child of the projectile; `isTrueDmg` just overrides `main.startSize` for the escalation read. The old `CreateProjectileHalo` and its pulse loop in [StructureProjectile.cs](Assets/Scripts/Pathogen/Combat/StructureProjectile.cs) are deleted — the ParticleSystem handles its own simulation.
    - **Artist workflow from now on:** menu → "Pathogen/Build VFX Prefabs" once to scaffold, then edit `Assets/Resources/VFX/StructureMuzzleFlash.prefab` in the Inspector like any other ParticleSystem. Shader preservation is automatic via the prefab→material→shader reference chain.
  12. Removed `Assets/TutorialInfo/` — Unity starter-project Readme sample. Its `ReadmeEditor` used `[InitializeOnLoad]` + `EditorApplication.delayCall` to auto-select the Readme asset, which ran `EditorStyles.*` outside an OnGUI context and spammed "Unable to use a named GUIStyle without a current skin" warnings whenever the hierarchy changed. No Pathogen code referenced it.
[18/04/2026]:
  1. Proposed 3D map work with AI:
    - Generate a full map in 3D with Meshy for visualisation and prototyping.
    - Upscale the concept art created by ChatGPT (original dimensions: 1330 × 1182):
      - GigaPixelAI: https://gigapixelai.com/ (Purchase required)
      - Upscayl: https://upscayl.org/ (✅ Free)
    - x4 the concept art image so that it fills in just enough from the 1330 x 1182 low resolution image. x8 that will make AI reinvent things and also on a mobile game, too much detail is not necessary.
    - Slice the image up and upscale each chunks individually for sharper details.
    - Tile them up.